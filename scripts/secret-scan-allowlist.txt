# secret-scan-allowlist.txt — false positive 허용 목록
#
# 포맷: 한 줄당 하나의 정규식 (grep -E).
# 매칭 대상: `path/to/file:lineno:line-content` 형식의 grep -n 출력.
# 패턴이 매칭되면 해당 히트는 민감정보가 아닌 것으로 간주.
# 주석(#), 빈 줄은 무시.
#
# 추가 규칙:
#   1. 정확한 라인을 복사하여 특징적인 부분만 살려라 (너무 넓으면 진짜 secret도 놓침)
#   2. 정규식 특수문자는 `\` 이스케이프 (. * ? + ( ) [ ] | 등)
#   3. 신규 FP 발생 시 파일명·이유 주석과 함께 추가

# autoloop/scripts/eval_analyzer.py: 스캐너가 감지해야 할 패턴 리스트를 코드 내에 정의 (메타 용법)
.*for pattern in \("api_key", "secret", "password", "token=", "bearer "\).*

# autoloop/scripts/improve_description.py: ANTHROPIC_API_KEY 환경변수명 언급 (실제 키 값 아님)
.*no separate ANTHROPIC_API_KEY needed.*
